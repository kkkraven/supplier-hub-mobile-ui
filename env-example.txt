# Supabase Configuration
# Получите эти значения в панели Supabase: Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Instructions:
# 1. Создайте файл .env.local в папке web/
# 2. Скопируйте содержимое этого файла в .env.local
# 3. Замените значения на реальные из вашего проекта Supabase
# 4. Убедитесь, что файл .env.local добавлен в .gitignore
